{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
  {{ range .Site.Pages }}
    {{ if not .Sitemap.Disable }}
    <url>
      <loc>{{ .Permalink }}</loc>
      {{ if not .Lastmod.IsZero }}<lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05Z07:00" }}</lastmod>{{ end }}
      {{ with .Sitemap.ChangeFreq }}<changefreq>{{ . }}</changefreq>{{ end }}
      {{ if ge .Sitemap.Priority 0.0 }}<priority>{{ .Sitemap.Priority }}</priority>{{ end }}
      {{ $seoImage := .Params.seo_image }}
      {{ with $seoImage }}
      <image:image>
        <image:loc>{{ cond (hasPrefix . "http") . (. | absURL) }}</image:loc>
        {{ with $.Params.seo_image_title }}<image:title>{{ . | htmlEscape }}</image:title>{{ end }}
        {{ with $.Params.seo_image_caption }}<image:caption>{{ . | htmlEscape }}</image:caption>{{ end }}
      </image:image>
      {{ end }}
    </url>
    {{ end }}
  {{ end }}
</urlset>